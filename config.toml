baseURL = "https://eallion.com"
# [en, zh-cn, fr, pl, ...] determines default content language
# [en, zh-cn, fr, pl, ...] 设置默认的语言
defaultContentLanguage = "zh-cn"
# theme
# 主题
theme = "DoIt"
# themes directory
# 主题目录
# themesDir = "../.."

# website title
# 网站标题
title = "大大的小蜗牛"

# whether to use robots.txt
# 是否使用 robots.txt
enableRobotsTXT = true
# whether to use git commit log
# 是否使用 git 信息
enableGitInfo = true
# whether to use emoji code
# 是否使用 emoji 代码
enableEmoji = true

ignoreFiles = ['data/douban']

[languages]
  [languages.en]
    title = "Charles Chin's Blog"
    contentDir = 'content/en'
    weight = 2
    # language code
    languageCode = "en"
    # language name
    languageName = "English"
    # whether to include Chinese/Japanese/Korean
    hasCJKLanguage = false
    # default amount of posts in each pages
    paginate = 12
    # [UA-XXXXXXXX-X] google analytics code (replaced by params.analytics.google)
    googleAnalytics = ""
    # copyright description used only for seo schema
    copyright = "This work is licensed under a Creative Commons Attribution-NonCommercial 4.0 International License."
    # Menu config
    [languages.en.params.header.title]
      name = "Charles Chin:~$"
      typeit = true
    [languages.en.menu]
      [[languages.en.menu.main]]
        identifier = "memos"
        pre = "<i class='fab fa-twitter'></i>"
        post = ""
        name = "Memos"
        url = "/memos/"
        title = "Memos"
        weight = "1"
      [[languages.en.menu.main]]
        identifier = "posts"
        # 你可以在名称 (允许 HTML 格式) 之前添加其他信息, 例如图标
        pre = ""
        # 你可以在名称 (允许 HTML 格式) 之后添加其他信息, 例如图标
        post = ""
        name = "Blog"
        url = "/en/posts/"
        # 当你将鼠标悬停在此菜单链接上时, 将显示的标题
        title = "All Article"
        weight = "2"
      [[languages.en.menu.main]]
        identifier = "movie"
        pre = ""
        post = ""
        name = "Watched"
        url = "/movie/"
        title = "DouBan Movie"
        weight = "3"
      [[languages.en.menu.main]]
        identifier = "categories"
        pre = ""
        post = ""
        name = "Cates"
        url = "/en/categories/"
        title = "Categories"
        weight = "4"
      [[languages.en.menu.main]]
        identifier = "links"
        name="Friends"
        pre = ""
        post = ""
        url="/links/"
        title = "Friends links"
        weight="5"
      [[languages.en.menu.main]]
        identifier = "about"
        pre = ""
        post = ""
        name = "About"
        url = "/about/"
        title = "About me"
        weight = "6"
      [[languages.en.menu.main]]
        identifier = "notbyai"
        pre = "<img class='notbyai' src='https://images.eallion.com/Not-By-AI/US/Written%20By%20Human/SVG/Written-By-Human-Not-By-AI-Badge-white.svg' alt='Written by human, not by AI'>"
        post = ""
        name=""
        url="https://notbyai.fyi"
        title = "Written by human, not by AI"
        weight="9"
    [languages.en.params]
      # site description
      description = "Chance favors the prepared mind."
      # site keywords
      keywords = ["eallion", "Charles", "chin", "blog", "Hugo", "蜗牛", "大大的小蜗牛", "博客", "网站"]
      # App icon config
      [languages.en.params.app]
        # optional site title override for the app when added to an iOS home screen or Android launcher
        title = "eallion"
        # whether to omit favicon resource links
        noFavicon = false
        # modern SVG favicon to use in place of older style .png and .ico files
        svgFavicon = "/eallion.png"
        # Safari mask icon color
        iconColor = "#5bbad5"
        # Windows v8-10 tile color
        tileColor = "#da532c"
      # Search config
      [languages.en.params.search]
        enable = true
        # type of search engine ("lunr", "algolia", "fuse")
        type = "fuse"
        # max index length of the chunked content
        contentLength = 4000
        # placeholder of the search bar
        placeholder = ""
        # max number of results length
        maxResultLength = 10
        # snippet length of the result
        snippetLength = 300
        # HTML tag name of the highlight part in results
        highlightTag = "em"
        # whether to use the absolute URL based on the baseURL in search index
        absoluteURL = false
        [languages.en.params.search.algolia]
          index = "en_index"
          appID = "5YGRNRQK1G"
          searchKey = "0ff6874805de24b84aa1d5ebccad56cd"
        [languages.en.params.search.fuse]
          # https://fusejs.io/api/options.html
          isCaseSensitive = false
          minMatchCharLength = 2
          findAllMatches = false
          location = 0
          threshold = 0.3
          distance = 100
          ignoreLocation = true
          useExtendedSearch = false
          ignoreFieldNorm = false
      # Home page config
      [languages.en.params.home]
        # amount of RSS pages
        rss = 10
        # Home page profile
        [languages.en.params.home.profile]
          enable = true
          # Gravatar Email for preferred avatar in home page
          # gravatarEmail = ""
          # URL of avatar shown in home page
          avatarURL = "/eallion.png"
          # title shown in home page (HTML format is supported)
          title = "Charles Chin's Blog"
          # subtitle shown in home page
          subtitle = "Chance favors the prepared mind."
          # whether to use typeit animation for subtitle
          typeit = false
          # whether to show social links
          social = true
          # disclaimer (HTML format is supported)
          disclaimer = "<div id='ticker'></div>"
        # Home page posts
        [languages.en.params.home.posts]
          enable = true
          # special amount of posts in each home posts page
          paginate = 10
      # Social config in home page
      [languages.en.params.social]
        Angellist = ""
        Bandcamp = ""
        Behance = ""
        Bilibili = ""
        Bitbucket = ""
        BuyMeACoffee = ""
        Codepen = ""
        Deviantart = ""
        Devto = ""
        Douban = ""
        Dribbble = ""
        Email = "eallions@gmail.com"
        Facebook = ""
        Fivehundredpx = ""
        Flickr = ""
        Foursquare = ""
        FreeCodeCamp = ""
        Gitea = ""
        GitHub = "eallion"
        Gitlab = ""
        Goodreads = ""
        Googlescholar = ""
        Hackernews = ""
        Instagram = ""
        Jsfiddle = ""
        Keybase = "eallion"
        Kickstarter = ""
        Ko-Fi = ""
        Lastfm = ""
        Liberapay = ""
        Linkedin = ""
        Linktree = ""
        Matrix = ""
        Medium = ""
        Mix = ""
        Odnoklassniki = ""
        ORCID = ""
        Patreon = ""
        Paypal = ""
        Pinterest = ""
        QQ = ""
        QQGroup = ""
        Quora = ""
        Reddit = ""
        Researchgate = ""
        Skype = ""
        Slidershare = ""
        Snapchat = ""
        Soundcloud = ""
        Spotify = "91qyot69mzbkp5inpdgrp3jej"
        Stackoverflow = "11409594"
        Steam = "eallion"
        Strava = ""
        Telegram = "eallion"
        Thingiverse = ""
        Tumblr = ""
        Twitch = ""
        Twitter = "eallion"
        Vine = ""
        VK = ""
        Weibo = "eallion"
        Whatsapp = ""
        Wordpress = ""
        Xing = ""
        XMPP = ""
        Youtubechannel = ""
        Youtubecustom = ""
        Youtubelegacy = ""
        Zhihu = "woniu"
        RSS = false
        [languages.en.params.social.Mastodon]
          id = "@eallion"
          prefix = "https://mastodon.social"
        [languages.en.params.social.Diaspora]
          #id = ""
          #prefix = ""
      # Sponsor config
      [languages.en.params.sponsor]
        enable = true
        bio = "If you find this post helpful, please consider sponsoring."
        link = "https://www.buymeacoffee.com/eallion/"
        # custom = "<a href=\"https://www.buymeacoffee.com/PCloud\" target=\"_blank\"><img src=\"https://cdn.buymeacoffee.com/buttons/v2/default-yellow.png\" alt=\"Buy Me A Coffee\" style=\"height: 40px !important;width: 145 !important;\" ></a>"


  [languages.zh-cn]
    weight = 1
    # 网站语言, 仅在这里 CN 大写
    languageCode = "zh-CN"
    # 语言名称
    languageName = "简体中文"
    # 是否包括中日韩文字
    hasCJKLanguage = true
    # 默认每页列表显示的文章数目
    paginate = 12
    # [UA-XXXXXXXX-X] 谷歌分析代号 (被 params.analytics.google 替代)
    googleAnalytics = ""
    # 版权描述，仅仅用于 SEO
    copyright = "This work is licensed under a Creative Commons Attribution-NonCommercial 4.0 International License."
    # 菜单配置
    [languages.zh-cn.menu]
      [[languages.zh-cn.menu.main]]
        identifier = "memos"
        pre = "<i class='fab fa-twitter'></i>"
        post = ""
        name = "嘀咕"
        url = "/memos/"
        title = "嘀咕 Memos"
        weight = "1"
      [[languages.zh-cn.menu.main]]
        identifier = "posts"
        # 你可以在名称 (允许 HTML 格式) 之前添加其他信息, 例如图标
        pre = ""
        # 你可以在名称 (允许 HTML 格式) 之后添加其他信息, 例如图标
        post = ""
        name = "博客"
        url = "/posts/"
        # 当你将鼠标悬停在此菜单链接上时, 将显示的标题
        title = "所有文章"
        weight = "2"
      [[languages.zh-cn.menu.main]]
        identifier = "movie"
        pre = ""
        post = ""
        name = "观影"
        url = "/movie/"
        title = "豆瓣观影记录"
        weight = "3"
      [[languages.zh-cn.menu.main]]
        identifier = "categories"
        pre = ""
        post = ""
        name = "分类"
        url = "/categories/"
        title = "博客分类"
        weight = "4"
      [[languages.zh-cn.menu.main]]
        identifier = "links"
        name="友情链接"
        pre = ""
        post = ""
        url="/links/"
        title = "友情链接"
        weight="5"
      [[languages.zh-cn.menu.main]]
        identifier = "about"
        pre = ""
        post = ""
        name = "关于"
        url = "/about/"
        title = "关于我关于本站"
        weight = "6"
      [[languages.zh-cn.menu.main]]
        identifier = "notbyai"
        pre = "<img class='notbyai' src='https://images.eallion.com/Not-By-AI/US/Written%20By%20Human/SVG/Written-By-Human-Not-By-AI-Badge-white.svg' alt='Written by human, not by AI'>"
        post = ""
        name=""
        url="https://notbyai.fyi"
        title = "Written by human, not by AI"
        weight="9"
    [languages.zh-cn.params]
      # 网站描述
      description = "机会总是垂青于有准备的人"
      # 网站关键词
      keywords = ["eallion", "Charles", "chin", "blog", "Hugo", "蜗牛", "大大的小蜗牛", "博客", "网站"]
      # 应用图标配置
      [languages.zh-cn.params.app]
        # 当添加到 iOS 主屏幕或者 Android 启动器时的标题, 覆盖默认标题
        title = "大大的小蜗牛"
        # 是否隐藏网站图标资源链接
        noFavicon = false
        # 更现代的 SVG 网站图标, 可替代旧的 .png 和 .ico 文件
        svgFavicon = "/eallion.png"
        # Safari 图标颜色
        iconColor = "#5bbad5"
        # Windows v8-10 磁贴颜色
        tileColor = "#da532c"
      # 搜索配置
      [languages.zh-cn.params.search]
        enable = true
        # 搜索引擎的类型 ("lunr", "algolia", "fuse")
        type = "fuse"
        # 文章内容最长索引长度
        contentLength = 4000
        # 搜索框的占位提示语
        placeholder = ""
        # 最大结果数目
        maxResultLength = 10
        # 结果内容片段长度
        snippetLength = 50
        # 搜索结果中高亮部分的 HTML 标签
        highlightTag = "em"
        # 是否在搜索索引中使用基于 baseURL 的绝对路径
        absoluteURL = false
        [languages.zh-cn.params.search.algolia]
          index = "zh_cn_index"
          appID = "5YGRNRQK1G"
          searchKey = "0ff6874805de24b84aa1d5ebccad56cd"
        [languages.zh-cn.params.search.fuse]
          # https://fusejs.io/api/options.html
          isCaseSensitive = false
          minMatchCharLength = 2
          findAllMatches = false
          location = 0
          threshold = 0.3
          distance = 100
          ignoreLocation = false
          useExtendedSearch = false
          ignoreFieldNorm = false
      # 主页信息设置
      [languages.zh-cn.params.home]
        # RSS 文章数目
        rss = 10
        # 主页个人信息
        [languages.zh-cn.params.home.profile]
          enable = true
          # Gravatar 邮箱，用于优先在主页显示的头像
          # gravatarEmail = ""
          # 主页显示头像的 URL
          avatarURL = "https://eallion.com/eallion.png"
          # 主页显示的网站标题 (支持 HTML 格式)
          title = "大大的小蜗牛"
          # 主页显示的网站副标题
          subtitle = "机会总是垂青于有准备的人"
          # 是否为副标题显示打字机动画
          typeit = false
          # 是否显示社交账号
          social = true
          # 免责声明 (支持 HTML 格式)
          disclaimer = "<div id='ticker'></div>"
        # 主页文章列表
        [languages.zh-cn.params.home.posts]
          enable = true
          # 主页每页显示文章数量
          paginate = 10
      # 主页的社交信息设置
      [languages.zh-cn.params.social]
        Angellist = ""
        Bandcamp = ""
        Behance = ""
        Bilibili = ""
        Bitbucket = ""
        BuyMeACoffee = ""
        Codepen = ""
        Deviantart = ""
        Devto = ""
        Douban = ""
        Dribbble = ""
        Email = "eallions@gmail.com"
        Facebook = ""
        Fivehundredpx = ""
        Flickr = ""
        Foursquare = ""
        FreeCodeCamp = ""
        Gitea = ""
        GitHub = "eallion"
        Gitlab = ""
        Goodreads = ""
        Googlescholar = ""
        Hackernews = ""
        Instagram = "eallion"
        Jsfiddle = ""
        Keybase = "eallion"
        Kickstarter = ""
        Ko-Fi = ""
        Lastfm = ""
        Liberapay = ""
        Linkedin = ""
        Linktree = ""
        Mastodon = "@eallion"
        Matrix = ""
        Medium = ""
        Mix = ""
        Odnoklassniki = ""
        ORCID = ""
        Patreon = ""
        Paypal = ""
        Pinterest = ""
        QQ = ""
        QQGroup = ""
        Quora = ""
        Reddit = ""
        Researchgate = ""
        Skype = ""
        Slidershare = ""
        Snapchat = ""
        Soundcloud = ""
        Spotify = "91qyot69mzbkp5inpdgrp3jej"
        Stackoverflow = "11409594"
        Steam = "eallion"
        Strava = ""
        Telegram = "eallion"
        Thingiverse = ""
        Tumblr = ""
        Twitch = ""
        Twitter = "eallion"
        Vine = ""
        VK = ""
        Weibo = ""
        Whatsapp = ""
        Wordpress = ""
        Xing = ""
        XMPP = ""
        Youtubechannel = ""
        Youtubecustom = ""
        Youtubelegacy = ""
        Zhihu = "woniu"
        RSS = false
        # 赞助设置
      [languages.zh-cn.params.sponsor]
        enable = true
        bio = "数字花园，用爱发电！"
        link = "https://afdian.net/a/eallion"
        # custom = "<a href=\"https://www.buymeacoffee.com/PCloud\" target=\"_blank\"><img src=\"https://cdn.buymeacoffee.com/buttons/v2/default-yellow.png\" alt=\"Buy Me A Coffee\" style=\"height: 40px !important;width: 145 !important;\" ></a>"


[params]
  # website title
  # 网站标题
  title = "大大的小蜗牛"
  # DoIt theme version
  # DoIt 主题版本
  version = "0.4.X"
  # site default theme ("light", "dark", "black", "auto")
  # 网站默认主题 ("light", "dark", "black", "auto")
  defaultTheme = "auto"
  # public git repo url only then enableGitInfo is true
  # 公共 git 仓库路径，仅在 enableGitInfo 设为 true 时有效
  gitRepo = "https://github.com/eallion/eallion.com"
  # which hash function used for SRI, when empty, no SRI is used ("sha256", "sha384", "sha512", "md5")
  # 哪种哈希函数用来 SRI, 为空时表示不使用 SRI ("sha256", "sha384", "sha512", "md5")
  fingerprint = "sha256"
  # date format
  # 日期格式
  dateFormat = "2006-01-02"
  # website images for Open Graph and Twitter Cards
  # 网站图片, 用于 Open Graph 和 Twitter Cards
  images = ["eallion.png"]
  # enable PWA
  # 开启 PWA 支持
  enablePWA = true
  # license information
  # 许可协议信息 (支持 HTML 格式)
  license = '<i class="fab fa-creative-commons fa-fw"></i> <a rel="license external nofollow noopener noreffer" href="https://creativecommons.org/licenses/by-nc-sa/4.0/" target="_blank">BY-NC-SA 4.0</a>'
  # [Experimental] Bundle js
  bundle = true
  # Header config
  # 页面头部导航栏配置
  [params.header]
    # desktop header mode ("fixed", "normal", "auto")
    # 桌面端导航栏模式 ("fixed", "normal", "auto")
    desktopMode = "fixed"
    # mobile header mode ("fixed", "normal", "auto")
    # 移动端导航栏模式 ("fixed", "normal", "auto")
    mobileMode = "auto"
    # theme change mode ("switch", "select")
    # 主题切换模式 ("switch", "select")
    themeChangeMode = "select"
    # Header title config
    # 页面头部导航栏标题配置
    [params.header.title]
      # URL of the LOGO
      # LOGO 的 URL
      logo = ""
      # title name
      # 标题名称
      name = "大大的小蜗牛:~$ "
      # you can add extra information before the name (HTML format is supported), such as icons
      # 你可以在名称 (允许 HTML 格式) 之前添加其他信息, 例如图标
      pre = ""
      # you can add extra information after the name (HTML format is supported), such as icons
      # 你可以在名称 (允许 HTML 格式) 之后添加其他信息, 例如图标
      post = ""
      # whether to use typeit animation for title name
      # 是否为标题显示打字机动画
      typeit = true

  # Footer config
  # 页面底部信息配置
  [params.footer]
    enable = true
    # Custom content (HTML format is supported)
    # 自定义内容 (支持 HTML 格式)
    custom = ''
    # whether to show Hugo and theme info
    # 是否显示 Hugo 和主题信息
    hugo = true
    # Hosted on (HTML format is supported)
    # 托管服务信息 (支持 HTML 格式)
    # <a title="Github Pages" href="https://docs.github.com/en/pages/" target="_blank" rel="noopener noreffer">GitHub Pages</a>
    hostedOn = '<a title="腾讯云 OSS + CDN" href="https://cloud.tencent.com/act/cps/redirect?redirect=2446&cps_key=1f05a4ef7955bfdbc954f3f0a115c2e3&from=console" target="_blank" rel="noopener noreffer">腾讯云</a>'
    # whether to show copyright info
    # 是否显示版权信息
    copyright = true
    # whether to show the author
    # 是否显示作者
    author = false
    # site creation time
    # 网站创立年份
    since = 2006
    # ICP info only in China (HTML format is supported)
    # ICP 备案信息，仅在中国使用 (支持 HTML 格式)
    icp = '<i class="fas fa-balance-scale fa-fw"></i> <a title="ICP 备案号" href="https://beian.miit.gov.cn/" target="_blank" rel="noopener noreferrer">浙ICP备16031768号-2</a> | <a title="RSS Feed" href="https://eallion.com/index.xml" target="_blank" rel="noopener noreferrer"><i class="fas fa-rss fa-fw"></i></a> <a title="GitHub 源码" href="https://github.com/eallion/eallion.com" target="_blank" rel="noopener noreferrer"><i class="fas fa-code fa-fw"></i></a> <a title="Server Status" href="https://status.eallion.com/" target="_blank" rel="noopener noreferrer"><i class="fas fa-database fa-fw"></i></a> <a href="https://myssl.com/seal/detail?domain=eallion.com" title="本站支持SSL安全访问" target="_blank" rel="noopener noreferrer"><i class="fab fa-expeditedssl fa-fw"></i></a> <a href="https://ipw.cn/ipv6webcheck/?site=eallion.com" title="本站支持IPv6访问" target="_blank" rel="noopener noreferrer" class="icon-ipv6">IPv6</a>'
    # license info (HTML format is supported)
    # 许可协议信息 (支持 HTML 格式)
    license= "<a title='eallion.com 版权信息' href='https://eallion.com/copyright/' target='_blank' rel='noopener noreferrer'>eallion.com</a> All Rights Reserved."

  # Section (all posts) page config
  # Section (所有文章) 页面配置
  [params.section]
    # special amount of posts in each section page
    # section 页面每页显示文章数量
    paginate = 20
    # date format (month and day)
    # 日期格式 (月和日)
    dateFormat = "01-02"
    # amount of RSS pages
    # RSS 文章数目
    rss = 10
    # recently updated posts settings
    # 最近更新文章设置
    [params.section.recentlyUpdated]
      enable = true
      rss = true
      days = 30
      maxCount = 10

  # List (category or tag) page config
  # List (目录或标签) 页面配置
  [params.list]
    # special amount of posts in each list page
    # list 页面每页显示文章数量
    paginate = 20
    # date format (month and day)
    # 日期格式 (月和日)
    dateFormat = "01-02"
    # amount of RSS pages
    # RSS 文章数目
    rss = 10

  # Page config
  # 文章页面配置
  [params.page]
    # whether to hide a page from home page
    # 是否在主页隐藏一篇文章
    hiddenFromHomePage = false
    # whether to hide a page from search results
    # 是否在搜索结果中隐藏一篇文章
    hiddenFromSearch = false
    # whether to enable twemoji
    # 是否使用 twemoji
    twemoji = false
    # whether to enable lightgallery
    # 是否使用 lightgallery
    lightgallery = true
    # whether to enable the ruby extended syntax
    # 是否使用 ruby 扩展语法
    ruby = true
    # whether to enable the fraction extended syntax
    # 是否使用 fraction 扩展语法
    fraction = true
    # whether to enable the fontawesome extended syntax
    # 是否使用 fontawesome 扩展语法
    fontawesome = true
    # whether to show link to Raw Markdown content of the content
    # 是否显示原始 Markdown 文档内容的链接
    linkToMarkdown = true
    # configure the link to view source the post
    # 配置文章原始文件的链接
    linkToSource = "https://github.com/eallion/eallion.com/blob/main/content/{path}"
    # "https://github.com/user/repo/blob/main/{path}"
    # configure the link to edit the post
    # 配置编辑文章的链接
    # linkToEdit = "https://github.com/eallion/eallion.com/edit/main/content/{path}"
    # https://github.com/user/repo/edit/main/{path}
    # https://gitlab.com/user/repo/-/edit/main/{path}
    # https://bitbucket.org/user/repo/src/main/{path}?mode=edit
    # configure the link to report issue for the post
    # 配置提交错误的链接
    linkToReport = "https://github.com/eallion/eallion.com/issues/new?title=[bug]%20{title}&body=|Field|Value|%0A|-|-|%0A|Title|{title}|%0A|Url|{url}|%0A|Filename|https://github.com/eallion/eallion.com/blob/main/{path}|"
    # https://github.com/user/repo/issues/new?title=[bug]%20{title}&body=|Field|Value|%0A|-|-|%0A|Title|{title}|%0A|Url|{url}|%0A|Filename|https://github.com/user/repo/blob/main/{path}|"
    # https://gitlab.com/user/repo/-/issues/new?issue[title]=[bug]%20{title}&issue[description]=|Field|Value|%0A|-|-|%0A|Title|{title}|%0A|Url|{url}|%0A|Filename|https://gitlab.com/user/repo/-/edit/main/{path}|
    # whether to show the full text content in RSS
    # 是否在 RSS 中显示全文内容
    rssFullText = false
    # whether to enable series navigation
    # 是否使用系列导航
    seriesNavigation = true
    # Display a message at the beginning of an article to warn the reader that its content might be outdated.
    # 在文章开头显示提示信息，提醒读者文章内容可能过时。
    [params.page.outdatedArticleReminder]
      enable = true
      # Display the reminder if the last modified time is more than 90 days ago.
      # 如果文章最后更新于这天数之前，显示提醒
      reminder = 90
      # Display warning if the last modified time is more than 180 days ago.
      # 如果文章最后更新于这天数之前，显示警告
      warning = 180
    # Table of the contents config
    # 目录配置
    [params.page.toc]
      # whether to enable the table of the contents
      # 是否使用目录
      enable = true
      # whether to keep the static table of the contents in front of the post
      # 是否保持使用文章前面的静态目录
      keepStatic = false
      # whether to make the table of the contents in the sidebar automatically collapsed
      # 是否使侧边目录自动折叠展开
      auto = true
    # Code config
    # 代码配置
    [params.page.code]
      # whether to show the copy button of the code block
      # 是否显示代码块的复制按钮
      copy = true
      # the maximum number of lines of displayed code by default
      # 默认展开显示的代码行数
      maxShownLines = 1000
    # Table config
    # 表格配置
    [params.page.table]
      # whether to enable sorting in the tables
      # 是否开启表格排序
      sort = true
    [params.page.header]
      # whether to enable auto header numbering
      # 是否开启标题自动编号
      [params.page.header.number]
        enable = false
        [params.page.header.number.format]
          h2 = "{h2} {title}"
          h3 = "{h2}.{h3} {title}"
          h4 = "{h2}.{h3}.{h4} {title}"
          h5 = "{h2}.{h3}.{h4}.{h5} {title}"
          h6 = "{h2}.{h3}.{h4}.{h5}.{h6} {title}"
    # KaTeX mathematical formulas config (KaTeX https://katex.org/)
    # KaTeX 数学公式配置 (KaTeX https://katex.org/)
    [params.page.math]
      enable = false
      # default block delimiter is $$ ... $$ and \\[ ... \\]
      # 默认块定界符是 $$ ... $$ 和 \\[ ... \\]
      blockLeftDelimiter = ""
      blockRightDelimiter = ""
      # default inline delimiter is $ ... $ and \\( ... \\)
      # 默认行内定界符是 $ ... $ 和 \\( ... \\)
      inlineLeftDelimiter = ""
      inlineRightDelimiter = ""
      # KaTeX extension copy_tex
      # KaTeX 插件 copy_tex
      copyTex = true
      # KaTeX extension mhchem
      # KaTeX 插件 mhchem
      mhchem = true
    # Mapbox GL JS config (Mapbox GL JS https://docs.mapbox.com/mapbox-gl-js)
    # Mapbox GL JS 配置 (Mapbox GL JS https://docs.mapbox.com/mapbox-gl-js)
    [params.page.mapbox]
      # access token of Mapbox GL JS
      # Mapbox GL JS 的 access token
      accessToken = "pk.eyJ1IjoiZGlsbG9uenEiLCJhIjoiY2s2czd2M2x3MDA0NjNmcGxmcjVrZmc2cyJ9.aSjv2BNuZUfARvxRYjSVZQ"
      # style for the light theme
      # 浅色主题的地图样式
      lightStyle = "mapbox://styles/mapbox/light-v10?optimize=true"
      # style for the dark theme
      # 深色主题的地图样式
      darkStyle = "mapbox://styles/mapbox/dark-v10?optimize=true"
      # whether to add NavigationControl (https://docs.mapbox.com/mapbox-gl-js/api/#navigationcontrol)
      # 是否添加 NavigationControl (https://docs.mapbox.com/mapbox-gl-js/api/#navigationcontrol)
      navigation = true
      # whether to add GeolocateControl (https://docs.mapbox.com/mapbox-gl-js/api/#geolocatecontrol)
      # 是否添加 GeolocateControl (https://docs.mapbox.com/mapbox-gl-js/api/#geolocatecontrol)
      geolocate = true
      # whether to add ScaleControl (https://docs.mapbox.com/mapbox-gl-js/api/#scalecontrol)
      # 是否添加 ScaleControl (https://docs.mapbox.com/mapbox-gl-js/api/#scalecontrol)
      scale = true
      # whether to add FullscreenControl (https://docs.mapbox.com/mapbox-gl-js/api/#fullscreencontrol)
      # 是否添加 FullscreenControl (https://docs.mapbox.com/mapbox-gl-js/api/#fullscreencontrol)
      fullscreen = true
    # Social share links in post page
    # 文章页面的分享信息设置
    [params.page.share]
      enable = true
      Baidu = false
      Blogger = false
      Buffer = false
      Digg = false
      Evernote = true
      Facebook = true
      Flipboard = false
      HackerNews = false
      Instapaper = false
      Line = false
      Linkedin = false
      Mix = false
      Myspace = false
      Odnoklassniki = false
      Pinterest = false
      Pocket = false
      Reddit = false
      Renren = false
      Skype = false
      Stumbleupon = false
      Telegram = true
      Trello = false
      Tumblr = false
      Twitter = true
      VK = false
      Weibo = true
      Weixin= "https://api.eallion.com/qrcode/?size=131&text="
      Whatsapp = false
      Xing = false
    # Comment config
    # 评论系统设置
    [params.page.comment]
      enable = true
      # Disqus comment config (https://disqus.com/)
      # Disqus 评论系统设置 (https://disqus.com/)
      [params.page.comment.disqus]
        enable = false
        # Disqus shortname to use Disqus in posts
        # Disqus 的 shortname，用来在文章中启用 Disqus 评论系统
        shortname = ""
      # Gitalk comment config (https://github.com/gitalk/gitalk)
      # Gitalk 评论系统设置 (https://github.com/gitalk/gitalk)
      [params.page.comment.gitalk]
        enable = false
        owner = ""
        repo = ""
        clientId = ""
        clientSecret = ""
      # Valine comment config (https://github.com/xCss/Valine)
      # Valine 评论系统设置 (https://github.com/xCss/Valine)
      [params.page.comment.valine]
        enable = false
        appId = ""
        appKey = ""
        placeholder = ""
        avatar = "mp"
        meta= ""
        pageSize = 10
        lang = "en"
        visitor = true
        recordIP = false
        highlight = true
        enableQQ = false
        serverURLs = ""
        # emoji data file name, default is "google.yml"
        # ("apple.yml", "google.yml", "facebook.yml", "twitter.yml")
        # located in "themes/DoIt/assets/data/emoji/" directory
        # you can store your own data files in the same path under your project:
        # "assets/data/emoji/"
        # emoji 数据文件名称, 默认是 "google.yml"
        # ("apple.yml", "google.yml", "facebook.yml", "twitter.yml")
        # 位于 "themes/DoIt/assets/data/emoji/" 目录
        # 可以在你的项目下相同路径存放你自己的数据文件:
        # "assets/data/emoji/"
        emoji = ""
      # Waline comment config (https://waline.js.org)
      # Waline 评论系统设置 (https://waline.js.org)
      [params.page.comment.waline]
        enable = false
        serverURL = "https://waline-comment-doit.vercel.app/"
        pageview = true
        comment = true
        # emoji = ['https://cdn.jsdelivr.net/gh/walinejs/emojis/weibo']
        # meta = ['nick', 'mail', 'link']
        # requiredMeta = []
        # login = 'enable'
        # wordLimit = 0
        # pageSize = 10
        # imageUploader = false
        # highlighter = false
        # texRenderer = false
      # Facebook comment config (https://developers.facebook.com/docs/plugins/comments)
      # Facebook 评论系统设置 (https://developers.facebook.com/docs/plugins/comments)
      [params.page.comment.facebook]
        enable = false
        width = "100%"
        numPosts = 10
        appId = ""
        languageCode = ""
      # Telegram comments config (https://comments.app/)
      # Telegram comments 评论系统设置 (https://comments.app/)
      [params.page.comment.telegram]
        enable = false
        siteID = ""
        limit = 5
        height = ""
        color = ""
        colorful = true
        dislikes = false
        outlined = false
        dark = false
      # Commento comment config (https://commento.io/)
      # Commento comment 评论系统设置 (https://commento.io/)
      [params.page.comment.commento]
        enable = false
      # Utterances comment config (https://utteranc.es/)
      # Utterances comment 评论系统设置 (https://utteranc.es/)
      [params.page.comment.utterances]
        enable = false
        # owner/repo
        repo = ""
        issueTerm = "pathname"
        label = ""
        lightTheme = "github-light"
        darkTheme = "github-dark"
      # Twikoo comment config (https://twikoo.js.org/)
      # Twikoo comment 评论系统设置 (https://twikoo.js.org/)
      [params.page.comment.twikoo]
        enable = true
        envId = "https://api.eallion.com/twikoo"
        region = ""
        path = ""
        cloudbase = false # boolean : true / false. # 是否部署在腾讯云 CloudBase
        visitor = false
        commentCount = true
      # Vssue comment config (https://vssue.js.org//)
      # Vssue comment 评论系统设置 (https://vssue.js.org//)
      [params.page.comment.vssue]
        enable = false
        platform = "" # ("bitbucket", "gitea", "gitee", "github", "gitlab")
        owner = ""
        repo = ""
        clientId = ""
        clientSecret = ""
      # Remark42 comment config (https://remark42.com/)
      # Remark42 comment 评论系统设置 (https://remark42.com/)
      [params.page.comment.remark42]
        enable = false
        host = ""
        site_id = ""
        max_shown_comments = 15
        show_email_subscription = true
        simple_view = false
      # giscus comment config (https://giscus.app/)
      # giscus comment 评论系统设置 (https://giscus.app/)
      [params.page.comment.giscus]
        enable = false
        # owner/repo
        dataRepo = "eallion/eallion.com"
        dataRepoId = "MDEwOlJlcG9zaXRvcnkyNjI2Njk1MDg="
        dataCategory = "giscus"
        dataCategoryId = "DIC_kwDOD6gExM4CS9eF"
        dataMapping = "pathname"
        dataReactionsEnabled = "1"
        dataEmitMetadata = "0"
        dataInputPosition = "top"
        lightTheme = "light"
        darkTheme = "dark_dimmed"
        dataLang = "zh-CN"
    # Third-party library config
    # 第三方库配置
    [params.page.library]
      [params.page.library.css]
        # someCSS = "some.css"
        # [params.page.library.css.someOtherCSS]
        #   src = "someOther.css"
        #   defer = true
        #   attr = "customAttribute"
        # located in "assets/" 位于 "assets/"
        # Or 或者
        # someCSS = "https://cdn.example.com/some.css"
      [params.page.library.js]
        someJavaScript = "js/custom.min.js"
        # [params.page.library.js.someOtherJavaScript]
        #   src = "someOther.js"
        #   defer = false
        #   async = true
        #   attr = "customAttribute"
        # located in "assets/" 位于 "assets/"
        # Or 或者
        # someJavascript = "https://cdn.example.com/some.js"
    # Page SEO config
    # 页面 SEO 配置
    [params.page.seo]
      # image URL
      # 图片 URL
      images = []
      # Publisher info
      # 出版者信息
      [params.page.seo.publisher]
        name = "Charles Chin"
        logoUrl = "/eallion.png"
    # Related content config
    # 相关文章推荐配置
    [params.page.related]
      enable = false
      count = 5

  # Sponsor config
  # 赞助设置
  [params.sponsor]
    enable = true
    bio = "数字花园，用爱发电！"
    link = "https://afdian.net/a/eallion" # 你的赞赏页面的地址
    # custom = "<a href=\"https://www.buymeacoffee.com/PCloud\" target=\"_blank\"><img src=\"https://cdn.buymeacoffee.com/buttons/v2/default-yellow.png\" alt=\"Buy Me A Coffee\" style=\"height: 40px !important;width: 145 !important;\" ></a>"

  # TypeIt config
  # TypeIt 配置
  [params.typeit]
    # typing speed between each step (measured in milliseconds)
    # 每一步的打字速度 (单位是毫秒)
    speed = 100
    # blinking speed of the cursor (measured in milliseconds)
    # 光标的闪烁速度 (单位是毫秒)
    cursorSpeed = 1000
    # character used for the cursor (HTML format is supported)
    # 光标的字符 (支持 HTML 格式)
    cursorChar = "|"
    # cursor duration after typing finishing (measured in milliseconds, "-1" means unlimited)
    # 打字结束之后光标的持续时间 (单位是毫秒, "-1" 代表无限大)
    duration = -1

  # Site verification code for Google/Bing/Yandex/Pinterest/Baidu
  # 网站验证代码，用于 Google/Bing/Yandex/Pinterest/Baidu
  [params.verification]
    google = ""
    bing = ""
    yandex = ""
    pinterest = ""
    baidu = ""
    so = ""
    sogou = ""

  # Site SEO config
  # 网站 SEO 配置
  [params.seo]
    # image URL
    # 图片 URL
    image = "/images/Apple-Devices-Preview.png"
    # thumbnail URL
    # 缩略图 URL
    thumbnailUrl = "/images/screenshot.png"

  # Analytics config
  # 网站分析配置
  [params.analytics]
    enable = true
    # Google Analytics
    [params.analytics.google]
      id = ""
      # whether to anonymize IP
      # 是否匿名化用户 IP
      anonymizeIP = true
    # Fathom Analytics
    [params.analytics.fathom]
      id = ""
      # server url for your tracker if you're self hosting
      # 自行托管追踪器时的主机路径
      server = ""
    # Baidu Analytics
    [params.analytics.baidu]
      id = ""
    # Umami Analytics
    [params.analytics.umami]
      data_website_id = "cbd4a67a-82e8-48ed-9d04-a0e4815466f0"
      src = "https://api.eallion.com/umami/69d6ffe.js?v=2.2.0"
      data_host_url ="https://a.eallion.com"
      data_domains = "eallion.com,www.eallion.com"
    # Plausible Analytics
    [params.analytics.plausible]
      data_domain = ""
      src = ""
    # Cloudflare Analytics
    [params.analytics.cloudflare]
      token = ""
    # Splitbee Analytics
    [params.analytics.splitbee]
      enable = false
      # no cookie mode
      # 无cookie模式
      No_cookie = true
      # respect the do not track setting of the browser
      # 遵循浏览器的”要求网站不跟踪我“设定
      Do_not_track = true
      # token(optional), more info on https://splitbee.io/docs/embed-the-script
      # token(可选)，详细信息请前往 https://splitbee.io/docs/embed-the-script 查看
      data_token = ""

  # Cookie consent config
  # Cookie 许可配置
  [params.cookieconsent]
    enable = false
    # text strings used for Cookie consent banner
    # 用于 Cookie 许可横幅的文本字符串
    [params.cookieconsent.content]
      message = ""
      dismiss = ""
      link = ""

  # CDN config for third-party library files
  # 第三方库文件的 CDN 设置
  [params.cdn]
    # CDN data file name, disabled by default
    # ("jsdelivr.yml")
    # located in "themes/DoIt/assets/data/cdn/" directory
    # you can store your own data files in the same path under your project:
    # "assets/data/cdn/"
    # CDN 数据文件名称, 默认不启用
    # ("jsdelivr.yml")
    # 位于 "themes/DoIt/assets/data/cdn/" 目录
    # 可以在你的项目下相同路径存放你自己的数据文件:
    # "assets/data/cdn/"
    data = ""

  # Compatibility config
  # 兼容性设置
  [params.compatibility]
    # whether to use Polyfill.io to be compatible with older browsers
    # 是否使用 Polyfill.io 来兼容旧式浏览器
    polyfill = true
    # whether to use object-fit-images to be compatible with older browsers
    # 是否使用 object-fit-images 来兼容旧式浏览器
    objectFit = true

# Markup related configuration in Hugo
# Hugo 解析文档的配置
[markup]
  # Syntax Highlighting (https://gohugo.io/content-management/syntax-highlighting)
  # 语法高亮设置 (https://gohugo.io/content-management/syntax-highlighting)
  [markup.highlight]
    codeFences = true
    guessSyntax = true
    lineNos = true
    lineNumbersInTable = true
    lineNoStart = 1
    tabWidth = 2
    # false is a necessary configuration (https://github.com/dillonzq/LoveIt/issues/158)
    # false 是必要的设置 (https://github.com/dillonzq/LoveIt/issues/158)
    noClasses = false
  # Goldmark is from Hugo 0.60 the default library used for Markdown
  # Goldmark 是 Hugo 0.60 以来的默认 Markdown 解析库
  [markup.goldmark]
    [markup.goldmark.extensions]
      definitionList = true
      footnote = true
      linkify = true
      strikethrough = true
      table = true
      taskList = true
      typographer = true
    [markup.goldmark.parser]
      autoHeadingID = true
      autoHeadingIDType = 'github'
      wrapStandAloneImageWithinParagraph = true
      [markup.goldmark.parser.attribute]
        block = true
        title = true
    [markup.goldmark.renderer]
      # whether to use HTML tags directly in the document
      # 是否在文档中直接使用 HTML 标签
      unsafe = true
      hardWraps = true
  # Table Of Contents settings
  # 目录设置
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 6

# Author config
# 作者配置
[author]
  name = "Charles Chin"
  email = "eallions@gmail.com"
  link = "https://eallion.com/"
  avatar = "/eallion.png"
  #gravatarEmail = ""

[frontmatter]
  lastmod = [ 'lastmod', ':git', 'date']

# Sitemap config
# 网站地图配置
[sitemap]
  changefreq = "weekly"
  filename = "sitemap.xml"
  priority = 0.5

# Permalinks config (https://gohugo.io/content-management/urls/#permalinks)
# Permalinks 配置 (https://gohugo.io/content-management/urls/#permalinks)
[Permalinks]
  # posts = ":year/:month/:filename"
  # posts = ":filename"
  posts = "/:slug/" # Don't miss the last slash: "/"
  tags = "/tags/:slug/" # Don't miss the last slash: "/"

# Privacy config (https://gohugo.io/about/hugo-and-gdpr/)
# 隐私信息配置 (https://gohugo.io/about/hugo-and-gdpr/)
[privacy]
  # privacy of the Google Analytics (replaced by params.analytics.google)
  # Google Analytics 相关隐私 (被 params.analytics.google 替代)
  [privacy.googleAnalytics]
    # ...
  [privacy.twitter]
    enableDNT = true
  [privacy.youtube]
    privacyEnhanced = true

# Options to make output .md files
# 用于输出 Markdown 格式文档的设置
[mediaTypes]
  [mediaTypes."text/plain"]
    suffixes = ["md"]

# Options to make output .md files
# 用于输出 Markdown 格式文档的设置
[outputFormats.MarkDown]
  mediaType = "text/plain"
  isPlainText = true
  isHTML = false

# Options to make hugo output files
# 用于 Hugo 输出文档的设置
[outputs]
  home = ["HTML", "RSS", "JSON"]
  page = ["HTML", "MarkDown"]
  section = ["HTML", "RSS"]
  taxonomy = ["HTML", "RSS"]
  taxonomyTerm = ["HTML"]

# Options for taxonomies
# 用于分类的设置
[taxonomies]
  author = "authors"
  category = "categories"
  tag = "tags"
  series = "series"
  slug = "slug"
